spring:
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: false

  flyway:
    enabled: true

app:
  security:
    users:
      - name: user
        password: pass
        role: USER

clients:
  member:
    url: http://localhost:1080
